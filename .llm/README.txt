.llm/ — agent/design docs for this repo.

Put longer-form design / implementation notes here, one feature per file, under
an appropriately-named subdir, e.g.:
  .llm/implementation/<feature>.md

Conventions (match the rest of the repo's docs):
- Every doc carries a "last updated: YYYY-MM-DD" date and is kept in sync with
  the code it describes (see CLAUDE.md / .github/copilot-instructions.md).
- Park features as DISABLED with a date instead of deleting their docs.

Don't scatter instruction-style docs in the repo root — they live here, except
AGENTS.md (repo root, auto-discovered) and .github/copilot-instructions.md.
